name: healthcare_blockchain_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ajusta según tu SDK de Flutter

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0 # Para gestión de estado
  http: ^1.0.0     # Para llamadas HTTP (necesitarás configurar esto más adelante)
  qr_flutter: ^4.1.0 # Para generar QR (App Paciente)
  mobile_scanner: ^5.1.0 # Para escanear QR (App Doctor)
  # Descomenta y añade otros paquetes cuando los necesites:
  # qr_code_scanner: ^any
  # table_calendar: ^any
  # flutter_secure_storage: ^any # Para almacenar tokens de forma segura

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg